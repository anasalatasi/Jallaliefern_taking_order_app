import 'package:flutter/material.dart';

class InProg_Page extends StatelessWidget {
  const InProg_Page({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Row(
        children: [
          Text(
            'Hi2ew12rhflusd    dadadodo'
          )
        ],
      ),
    );
  }
}